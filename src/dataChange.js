import arrayData from './arrayData';

averageCreditCardNum(arrayData);

// eslint-disable-next-line no-unused-vars
export function averageCreditCardNum(arr) {
  //average credit card number per country

  // const keys = Object.keys(arr[0]);


  // const col = keys[6];

  // const col = keys.map((key) => {
  //   return {
  //     key: key,
  //     card: key[6],
  //   };
  // });

  // const columns = keys.map((key) => {
  //   return {
  //     key: key,
  //     card: key.map((num) => num),
  //   };
  // });
  // return columns;

  // const red = arr.reduce((acc, curr) => {
  //   acc[curr.col];
  // }, {});

}
